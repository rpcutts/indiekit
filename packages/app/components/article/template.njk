{%- set classes = "article" + (" " + opts.classes if opts.classes) -%}
<article class="{{ classes }}">
{%- if opts.title %}
<h2 class="article__title">{{ opts.title }}</h2>
{%- endif %}
{{ opts.content | markdown | safe }}
</article>