{% extends "_document.njk" %}

{% set title = __("config.title %s", app.name) %}

{% block content %}
  {{ summary({
    title: __("config.app.title"),
    actions: [{
      href: "/config/app?referrer=/config",
      text: __("Edit")
    }],
    rows: [{
      key: {
        text: __("config.app.locale.label")
      },
      value: {
        text: __("locale." + app.locale)
      }
    }, {
      key: {
        text: __("config.app.publisher.label")
      },
      value: {
        text: __("publisher." + app.publisher)
      }
    }, {
      key: {
        text: __("config.app.themeColor.label")
      },
      value: {
        text: "<span style=\"color:" + app.themeColor + "\" aria-hidden\"true\">●</span> " + app.themeColor
      }
    }]
  }) }}

  {{ summary({
    title: __("config.github.title"),
    actions: [{
      href: "/config/github?referrer=/config",
      text: __("Edit")
    }],
    rows: [{
      key: {
        text: __("config.github.user.label")
      },
      value: {
        text: github.user
      }
    }, {
      key: {
        text: __("config.github.repo.label")
      },
      value: {
        text: github.repo
      }
    }, {
      key: {
        text: __("config.github.branch.label")
      },
      value: {
        text: github.branch
      }
    }, {
      key: {
        text: __("config.github.token.label")
      },
      value: {
        text: github.token
      }
    }]
  }) }}

  {{ summary({
    title: __("config.pub.title"),
    actions: [{
      href: "/config/app?referrer=/config",
      text: __("Edit")
    }],
    rows: [{
      key: {
        text: __("config.pub.me.label")
      },
      value: {
        text: pub.me
      }
    }]
  }) }}

  {%- set bookmarklet %}
    {% set bookmarkletFunction -%}
      (function() {
        var title = document.getElementsByTagName('title')[0].innerHTML;
        name = encodeURIComponent(title);

        var selection = '';
        if (window.getSelection) {
          selection = window.getSelection().toString();
        } else if (document.selection && document.selection.type != 'Control') {
          selection = document.selection.createRange().text;
        }
        content = encodeURIComponent(selection);

        new_window=window.open(
          '{{ app.url }}/share/bookmarklet?name='+name+'&content='+content+'&url='+encodeURIComponent(document.location.href),
          'Sharer',
          'resizable,scrollbars,status=0,toolbar=0,menubar=0,titlebar=0,width=640,height=720,location=0'
        );
      })();
    {%- endset %}
    {% set bookmarkletHref = "javascript:" + bookmarkletFunction | replace("  ", "") | replace("\n", "") | replace("&", "&amp;") | trim %}
    {{ bookmarklet({
      href: bookmarkletHref,
      text: __("Share page")
    }) if not minimalui }}
  {%- endset %}

  {{ summary({
    title: __("config.share.title"),
    rows: [{
      key: {
        text: __("config.share.bookmarklet.label")
      },
      value: {
        text: __("config.share.bookmarklet.drag") + ": " + bookmarklet
      }
    }]
  }) }}
{% endblock %}