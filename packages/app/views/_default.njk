<!DOCTYPE html>
<html lang="{{ locale }}" dir="ltr" class="h-x-app">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"/>
    <meta name="theme-color" content="{{ app.themeColor }}"/>
    <link rel="mask-icon" href="{{ app.url }}/logo.svg" color="{{ app.themeColor }}" class="u-logo"/>
    <link rel="stylesheet" href="{{ app.url }}/app.css"/>
    <link rel="stylesheet" href="{{ app.url }}/app.css"/>
    <title>{{ title + " - " if title }}{{ app.name }}</title>
  </head>
  <body>
    <header class="banner">
      <h1 class="banner__title">
        <a class="u-url" href="{{ app.url }}">
          {% include 'logo.svg' %}
          <span class="p-name">{{ app.name }}</span>
        </a>
      </h1>
      <p class="banner__description p-summary">{{ app.description }}</p>
      <nav class="banner__navigation" aria-label="site">
        <a href="/docs">Docs</a>
      {% if session.me %}
        <a href="/sign-out">Sign out</a>
      {% else %}
        <a href="/sign-in">Sign in</a>
      {% endif %}
      </nav>
    </header>
    <main class="main">
      {% block main %}{% endblock %}
    </main>
    <footer class="contentinfo">
      <a href="{{ app.repository.url | replace('.git', '') }}/releases/tag/v{{ app.version }}">v{{ app.version }}</a>
    </footer>
  </body>
  {% block scripts %}{% endblock %}
</html>
